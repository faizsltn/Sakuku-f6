<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sakuku - Elegant Edition</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-dark: #1a1c20; /* Latar belakang aplikasi utama */
            --card-light: #9ca3af; /* Warna abu-abu pada kartu sesuai gambar */
            --primary-accent: #fbbf24; 
            --text-dark: #1f2937;
            --input-bg: #1a1c20;
        }

        body {
            background-color: var(--bg-dark);
            font-family: 'Poppins', sans-serif;
            color: #ffffff;
            min-height: 100vh;
        }

        .hero-section {
            padding: 60px 0;
            text-align: center;
        }

        .hero-title {
            font-weight: 700;
            font-size: 3rem;
            color: var(--primary-accent);
        }

        /* CARD TAMBAH TRANSAKSI */
        .organic-card {
            background: var(--card-light); /* Sekarang berwarna abu-abu terang */
            border-radius: 30px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
            color: var(--text-dark); /* Teks judul jadi gelap */
            border: none;
        }

        .form-label {
            font-weight: 600;
            color: #374151;
            font-size: 0.9rem;
        }

        .form-control, .form-select {
            border-radius: 12px;
            border: none;
            padding: 12px;
            background-color: var(--input-bg); /* Input tetap gelap agar kontras */
            color: #d1d5db;
        }

        .form-control::placeholder {
            color: #4b5563;
        }

        .btn-organic {
            background-color: #f59e0b;
            color: #000;
            border: none;
            border-radius: 12px;
            padding: 12px 45px;
            font-weight: 700;
            box-shadow: 0 4px 15px rgba(245, 158, 11, 0.4);
        }

        /* TABEL RIWAYAT */
        .table-container {
            border-radius: 25px;
            overflow: hidden;
            background: #2d3035; /* Header tabel tetap gelap */
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .table-container h4 {
            padding: 25px 25px 0 25px;
            color: white;
        }

        .table {
            margin-bottom: 0;
        }

        .table thead th {
            background-color: #374151;
            color: var(--primary-accent);
            border: none;
            padding: 15px;
            font-size: 0.8rem;
        }

        /* Isi Tabel Putih Sesuai Gambar */
        .table tbody {
            background-color: #ffffff; 
        }

        .table tbody td {
            padding: 15px;
            color: #1f2937;
            border-bottom: 1px solid #e5e7eb;
            vertical-align: middle;
        }

        .badge-category {
            background-color: #fffbeb;
            color: #b45309;
            padding: 6px 12px;
            border-radius: 8px;
            font-weight: 600;
            border: 1px solid #fde68a;
        }

        .btn-action {
            border-radius: 8px;
            font-weight: 600;
        }
    </style>
</head>
<body>

    <div class="hero-section">
        <div class="container">
            <h1 class="hero-title">💰 Sakuku 💰</h1>
            <p class="opacity-75 text-white">Kelola Keuangan dengan Efisien</p>
        </div>
    </div>

    <div class="container">
        <div class="row justify-content-center mb-5">
            <div class="col-lg-10">
                <div class="card organic-card">
                    <div class="card-body p-5">
                        <h4 class="mb-4 fw-bold text-center">Tambah Transaksi Baru</h4>
                        <form action="/add" method="POST" class="row g-4">
                            <div class="col-md-4">
                                <label class="form-label">Nominal (Rp)</label>
                                <input type="number" name="nominal" class="form-control" placeholder="40000" required>
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Kategori</label>
                                <input type="text" name="nama_kategori" class="form-control" placeholder="Makan, Main, dll" required>
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Tanggal</label>
                                <input type="date" name="tanggal" class="form-control" required>
                            </div>
                            <div class="col-md-8">
                                <label class="form-label">Keterangan Singkat</label>
                                <input type="text" name="keterangan" class="form-control" placeholder="Belanja bulanan...">
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Metode Pembayaran</label>
                                <select name="metode_pembayaran" class="form-select">
                                    <option value="Cash">Cash / Tunai</option>
                                    <option value="Bank/Transfer">Transfer Bank</option>
                                </select>
                            </div>
                            <div class="col-12 text-center mt-5">
                                <button type="submit" class="btn btn-organic">Simpan Transaksi</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>

        <div class="row justify-content-center pb-5">
            <div class="col-lg-11">
                <div class="table-container shadow">
                    <h4 class="mb-4 fw-bold">Riwayat Pengeluaran</h4>
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr class="text-center">
                                    <th>TANGGAL</th>
                                    <th>KATEGORI</th>
                                    <th>NOMINAL</th>
                                    <th>METODE</th>
                                    <th>KETERANGAN</th>
                                    <th>AKSI</th>
                                </tr>
                            </thead>
                            <tbody class="text-center">
                                <% if (typeof data !== 'undefined' && data.length > 0) { %>
                                    <% data.forEach(item => { %>
                                    <tr>
                                        <td class="small text-muted"><%= item.tanggal %></td>
                                        <td><span class="badge-category"><%= item.nama_kategori %></span></td>
                                        <td class="fw-bold">Rp <%= item.nominal.toLocaleString('id-ID') %></td>
                                        <td><%= item.metode_pembayaran %></td>
                                        <td class="text-muted"><%= item.keterangan %></td>
                                        <td>
                                            <div class="d-flex justify-content-center gap-2">
                                                <a href="/edit/<%= item.PengeluaranID %>" class="btn btn-sm btn-warning btn-action">Edit</a>
                                                <a href="/delete/<%= item.PengeluaranID %>" class="btn btn-sm btn-danger btn-action">Hapus</a>
                                            </div>
                                        </td>
                                    </tr>
                                    <% }) %>
                                <% } else { %>
                                    <tr>
                                        <td colspan="6" class="py-5 text-muted">Belum ada data transaksi.</td>
                                    </tr>
                                <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>