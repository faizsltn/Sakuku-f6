<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sakuku - Elegant Dark Edition</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-dark: #1a1c20; /* Warna dasar gelap */
            --card-dark: #2d3035; /* Warna kartu */
            --primary-accent: #fbbf24; /* Kuning emas seperti gambar */
            --text-light: #f8fafc;
            --text-muted: #94a3b8;
        }

        body {
            background-color: var(--bg-dark);
            font-family: 'Poppins', sans-serif;
            color: var(--text-light);
            min-height: 100vh;
        }

        /* Banner Header dengan Background Gambar Uang Gelap */
        .hero-section {
            padding: 80px 0;
            margin-bottom: 40px;
            border-radius: 0 0 50px 50px;
            position: relative;
            overflow: hidden;
            /* Ganti URL ini dengan link gambar uang gelap pilihan Anda */
            background: linear-gradient(rgba(0, 0, 0, 0.7), rgba(0, 0, 0, 0.7)), 
                        url('https://images.unsplash.com/photo-1580519542036-c47de6196ba5?q=80&w=2071&auto=format&fit=crop');
            background-size: cover;
            background-position: center;
        }

        .hero-title {
            font-weight: 700;
            font-size: 3.5rem;
            color: var(--primary-accent);
            /* Bayangan teks agar lebih terbaca di atas gambar */
            text-shadow: 2px 4px 10px rgba(0, 0, 0, 0.8);
        }

        .section-tag {
            background-color: var(--primary-accent);
            color: #000;
            padding: 5px 15px;
            border-radius: 8px;
            font-weight: 700;
            font-size: 0.8rem;
            margin-bottom: 15px;
            display: inline-block;
        }

        /* Desain Card Gelap */
        .organic-card {
            background: var(--card-dark);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 25px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
            overflow: hidden;
        }

        .form-label {
            font-weight: 600;
            color: var(--text-light);
            font-size: 0.9rem;
        }

        .form-control, .form-select {
            border-radius: 12px;
            border: 1px solid #40444b;
            padding: 12px;
            background-color: #1a1c20;
            color: white;
        }

        .form-control:focus {
            background-color: #1a1c20;
            color: white;
            border-color: var(--primary-accent);
            box-shadow: 0 0 0 0.25rem rgba(251, 191, 36, 0.25);
        }

        .btn-organic {
            background-color: var(--primary-accent);
            color: #000;
            border: none;
            border-radius: 12px;
            padding: 12px 35px;
            font-weight: 700;
            transition: all 0.3s ease;
        }

        .btn-organic:hover {
            transform: translateY(-2px);
            background-color: #f59e0b;
            box-shadow: 0 10px 20px rgba(251, 191, 36, 0.3);
        }

        /* Styling Tabel Gelap */
        .table-container {
            border-radius: 25px;
            overflow: hidden;
            background: var(--card-dark);
            padding: 25px;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .table {
            color: var(--text-light);
        }

        .table thead th {
            background-color: #3d4148;
            color: var(--primary-accent);
            border: none;
            padding: 15px;
            font-weight: 700;
            text-transform: uppercase;
            font-size: 0.8rem;
        }

        .table tbody td {
            padding: 15px;
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
            vertical-align: middle;
        }

        .badge-category {
            background-color: rgba(251, 191, 36, 0.15);
            color: var(--primary-accent);
            padding: 6px 12px;
            border-radius: 8px;
            font-weight: 600;
        }

        .btn-action {
            border-radius: 8px;
            font-weight: 600;
        }
    </style>
</head>
<body>

    <div class="hero-section text-center shadow">
        <div class="container">
            <h1 class="hero-title">💰 Sakuku 💰</h1>
            <p class="opacity-75">Kelola Keuangan dengan Efisien</p>
        </div>
    </div>

    <div class="container py-2">
        <div class="row justify-content-center mb-5">
            <div class="col-lg-10">
                <div class="card organic-card">
                    <div class="card-body p-5">
                        <h4 class="mb-4 fw-bold text-center">Tambah Transaksi Baru</h4>
                        <form action="/add" method="POST" class="row g-4">
                            <div class="col-md-4">
                                <label class="form-label">Nominal (Rp)</label>
                                <input type="number" name="nominal" class="form-control" placeholder="50000" required>
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Kategori</label>
                                <input type="text" name="nama_kategori" class="form-control" placeholder="Makan, Main, dll" required>
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Tanggal</label>
                                <input type="date" name="tanggal" class="form-control" required>
                            </div>
                            <div class="col-md-8">
                                <label class="form-label">Keterangan Singkat</label>
                                <input type="text" name="keterangan" class="form-control" placeholder="Belanja bulanan...">
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Metode Pembayaran</label>
                                <select name="metode_pembayaran" class="form-select">
                                    <option value="Cash">Cash / Tunai</option>
                                    <option value="Bank/Transfer">Transfer Bank</option>
                                </select>
                            </div>
                            <div class="col-12 text-center mt-5">
                                <button type="submit" class="btn btn-organic px-5">Simpan Transaksi</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>

        <div class="row justify-content-center pb-5">
            <div class="col-lg-11">
                <div class="table-container shadow">
                    <h4 class="mb-4 fw-bold">Riwayat Pengeluaran</h4>
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr class="text-center">
                                    <th>Tanggal</th>
                                    <th>Kategori</th>
                                    <th>Nominal</th>
                                    <th>Metode</th>
                                    <th>Keterangan</th>
                                    <th>Aksi</th>
                                </tr>
                            </thead>
                            <tbody class="text-center">
                                <% if (data && data.length > 0) { %>
                                    <% data.forEach(item => { %>
                                    <tr>
                                        <td class="small text-muted"><%= item.tanggal %></td>
                                        <td><span class="badge-category"><%= item.nama_kategori %></span></td>
                                        <td class="fw-bold">Rp <%= item.nominal.toLocaleString('id-ID') %></td>
                                        <td class="small"><%= item.metode_pembayaran %></td>
                                        <td class="text-muted"><%= item.keterangan %></td>
                                        <td>
                                            <div class="d-flex justify-content-center gap-2">
                                                <a href="/edit/<%= item.PengeluaranID %>" class="btn btn-sm btn-warning btn-action text-dark">Edit</a>
                                                <a href="/delete/<%= item.PengeluaranID %>" class="btn btn-sm btn-danger btn-action" onclick="return confirm('Hapus data ini?')">Hapus</a>
                                            </div>
                                        </td>
                                    </tr>
                                    <% }) %>
                                <% } else { %>
                                    <tr>
                                        <td colspan="6" class="py-5 text-muted italic">Belum ada data transaksi tersimpan.</td>
                                    </tr>
                                <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>