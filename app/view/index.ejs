<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sakuku - Elegant Light Edition</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-light: #f1f5f9; /* Abu-abu sangat muda bersih */
            --card-white: #ffffff;
            --primary-accent: #0ea5e9; /* Biru cerah agar senada dengan uang 50rb */
            --secondary-accent: #fbbf24; /* Kuning emas aksen */
            --text-main: #1e293b;
            --text-muted: #64748b;
        }

        body {
            background-color: var(--bg-light);
            font-family: 'Poppins', sans-serif;
            color: var(--text-main);
            min-height: 100vh;
        }

        /* Hero Section dengan Background Uang 50rb Cerah */
        .hero-section {
            padding: 80px 0;
            margin-bottom: 40px;
            border-radius: 0 0 50px 50px;
            position: relative;
            overflow: hidden;
            /* Menggunakan gambar uang 50rb dengan overlay cerah */
            background: linear-gradient(rgba(255, 255, 255, 0.6), rgba(255, 255, 255, 0.8)), 
                        url('https://images.unsplash.com/photo-1593672715438-d88a75639f24?q=80&w=2072&auto=format&fit=crop');
            background-size: cover;
            background-position: center;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
        }

        .hero-title {
            font-weight: 700;
            font-size: 3.5rem;
            color: #0369a1; /* Biru pekat */
            text-shadow: 1px 1px 4px rgba(255, 255, 255, 0.8);
        }

        /* Desain Card Putih Bersih */
        .organic-card {
            background: var(--card-white);
            border: none;
            border-radius: 25px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.07);
        }

        .form-label {
            font-weight: 600;
            color: var(--text-main);
            font-size: 0.9rem;
        }

        .form-control, .form-select {
            border-radius: 12px;
            border: 1px solid #e2e8f0;
            padding: 12px;
            background-color: #f8fafc;
            color: var(--text-main);
        }

        .form-control:focus {
            border-color: var(--primary-accent);
            box-shadow: 0 0 0 0.25rem rgba(14, 165, 233, 0.15);
        }

        .btn-organic {
            background-color: var(--primary-accent);
            color: #fff;
            border: none;
            border-radius: 12px;
            padding: 12px 35px;
            font-weight: 700;
            transition: all 0.3s ease;
        }

        .btn-organic:hover {
            transform: translateY(-2px);
            background-color: #0284c7;
            box-shadow: 0 10px 20px rgba(14, 165, 233, 0.3);
            color: #fff;
        }

        /* Styling Tabel Cerah */
        .table-container {
            border-radius: 25px;
            overflow: hidden;
            background: var(--card-white);
            padding: 25px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.07);
        }

        .table {
            color: var(--text-main);
        }

        .table thead th {
            background-color: #f1f5f9;
            color: #0369a1;
            border: none;
            padding: 15px;
            font-weight: 700;
            text-transform: uppercase;
            font-size: 0.8rem;
        }

        .table tbody td {
            padding: 15px;
            border-bottom: 1px solid #f1f5f9;
            vertical-align: middle;
        }

        .badge-category {
            background-color: #e0f2fe;
            color: #0369a1;
            padding: 6px 12px;
            border-radius: 8px;
            font-weight: 600;
        }

        .btn-action {
            border-radius: 8px;
            font-weight: 600;
        }
    </style>
</head>
<body>

    <div class="hero-section text-center">
        <div class="container">
            <h1 class="hero-title">💰 Sakuku 💰</h1>
            <p class="text-secondary fw-semibold">Kelola Keuangan dengan Elegan & Efisien</p>
        </div>
    </div>

    <div class="container py-2">
        <div class="row justify-content-center mb-5">
            <div class="col-lg-10">
                <div class="card organic-card">
                    <div class="card-body p-5">
                        <h4 class="mb-4 fw-bold text-center" style="color: #0369a1;">Tambah Transaksi Baru</h4>
                        <form action="/add" method="POST" class="row g-4">
                            <div class="col-md-4">
                                <label class="form-label">Nominal (Rp)</label>
                                <input type="number" name="nominal" class="form-control" placeholder="50000" required>
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Kategori</label>
                                <input type="text" name="nama_kategori" class="form-control" placeholder="Makan, Main, dll" required>
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Tanggal</label>
                                <input type="date" name="tanggal" class="form-control" required>
                            </div>
                            <div class="col-md-8">
                                <label class="form-label">Keterangan Singkat</label>
                                <input type="text" name="keterangan" class="form-control" placeholder="Belanja bulanan...">
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Metode Pembayaran</label>
                                <select name="metode_pembayaran" class="form-select">
                                    <option value="Cash">Cash / Tunai</option>
                                    <option value="Bank/Transfer">Transfer Bank</option>
                                </select>
                            </div>
                            <div class="col-12 text-center mt-5">
                                <button type="submit" class="btn btn-organic px-5">Simpan Transaksi</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>

        <div class="row justify-content-center pb-5">
            <div class="col-lg-11">
                <div class="table-container">
                    <h4 class="mb-4 fw-bold" style="color: #0369a1;">Riwayat Pengeluaran</h4>
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr class="text-center">
                                    <th>Tanggal</th>
                                    <th>Kategori</th>
                                    <th>Nominal</th>
                                    <th>Metode</th>
                                    <th>Keterangan</th>
                                    <th>Aksi</th>
                                </tr>
                            </thead>
                            <tbody class="text-center">
                                <% if (typeof data !== 'undefined' && data.length > 0) { %>
                                    <% data.forEach(item => { %>
                                    <tr>
                                        <td class="small text-muted"><%= item.tanggal %></td>
                                        <td><span class="badge-category"><%= item.nama_kategori %></span></td>
                                        <td class="fw-bold text-dark">Rp <%= item.nominal.toLocaleString('id-ID') %></td>
                                        <td class="small text-secondary"><%= item.metode_pembayaran %></td>
                                        <td class="text-muted"><%= item.keterangan %></td>
                                        <td>
                                            <div class="d-flex justify-content-center gap-2">
                                                <a href="/edit/<%= item.PengeluaranID %>" class="btn btn-sm btn-outline-warning btn-action text-dark">Edit</a>
                                                <a href="/delete/<%= item.PengeluaranID %>" class="btn btn-sm btn-outline-danger btn-action" onclick="return confirm('Hapus data ini?')">Hapus</a>
                                            </div>
                                        </td>
                                    </tr>
                                    <% }) %>
                                <% } else { %>
                                    <tr>
                                        <td colspan="6" class="py-5 text-muted italic">Belum ada data transaksi tersimpan.</td>
                                    </tr>
                                <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>