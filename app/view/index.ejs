<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sakuku - Elegant Light Edition</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-light: #eef2f7; 
            --card-white: #ffffff;
            --primary-blue: #0ea5e9;
            --accent-yellow: #fbbf24;
            --text-dark: #1e293b;
        }

        body {
            background-color: var(--bg-light);
            font-family: 'Poppins', sans-serif;
            color: var(--text-dark);
            min-height: 100vh;
        }

        /* Hero Section dengan Background Uang 50rb sesuai gambar */
        .hero-section {
            padding: 60px 0;
            margin-bottom: 40px;
            border-radius: 25px;
            margin-top: 20px;
            position: relative;
            overflow: hidden;
            /* Gambar uang Rp50.000 dengan overlay cerah */
            background: linear-gradient(rgba(255, 255, 255, 0.4), rgba(255, 255, 255, 0.5)), 
                        url('https://images.unsplash.com/photo-1593672715438-d88a75639f24?q=80&w=2072&auto=format&fit=crop');
            background-size: cover;
            background-position: center;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        }

        .section-tag {
            background-color: var(--accent-yellow);
            color: #000;
            padding: 4px 15px;
            border-radius: 8px;
            font-weight: 700;
            font-size: 0.75rem;
            display: inline-block;
            margin-bottom: 10px;
            text-transform: uppercase;
        }

        .hero-title {
            font-weight: 700;
            font-size: 3rem;
            color: #b45309; /* Warna coklat keemasan mirip teks Sakuku di gambar */
            margin-bottom: 5px;
        }

        /* Desain Card Tambah Transaksi */
        .organic-card {
            background: var(--card-white);
            border: none;
            border-radius: 20px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.05);
        }

        .form-label {
            font-weight: 600;
            color: var(--text-dark);
            font-size: 0.9rem;
        }

        .form-control, .form-select {
            border-radius: 10px;
            border: 1px solid #cbd5e1;
            padding: 10px;
            background-color: #ffffff;
        }

        .btn-organic {
            background-color: var(--accent-yellow);
            color: #000;
            border: none;
            border-radius: 10px;
            padding: 10px 30px;
            font-weight: 700;
            transition: 0.3s;
        }

        .btn-organic:hover {
            background-color: #f59e0b;
            transform: translateY(-2px);
        }

        /* Styling Tabel Pengeluaran sesuai screenshot */
        .table-container {
            border-radius: 20px;
            overflow: hidden;
            background: var(--card-white);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.05);
        }

        .table-header-custom {
            background-color: #334155; /* Warna gelap untuk header tabel */
            color: #ffffff;
            padding: 15px 25px;
            font-weight: 700;
        }

        .table {
            margin-bottom: 0;
        }

        .table thead th {
            background-color: #f8fafc;
            color: #64748b;
            border-bottom: 2px solid #e2e8f0;
            padding: 15px;
            font-size: 0.8rem;
            text-transform: uppercase;
        }

        .table tbody tr {
            background-color: #ffffff;
        }

        .table tbody td {
            padding: 15px;
            border-bottom: 1px solid #f1f5f9;
            vertical-align: middle;
        }

        .badge-category {
            background-color: #fef3c7;
            color: #92400e;
            padding: 5px 12px;
            border-radius: 6px;
            font-weight: 600;
            font-size: 0.85rem;
        }

        .btn-edit { background-color: #fbbf24; color: #000; border: none; }
        .btn-hapus { background-color: #ef4444; color: #fff; border: none; }
    </style>
</head>
<body>

    <div class="container">
        <div class="hero-section text-center">
            <h1 class="hero-title">💰 Sakuku 💰</h1>
            <p class="fw-medium text-dark">Kelola Keuangan dengan Efisien</p>
        </div>

        <div class="row justify-content-center mb-5">
            <div class="col-lg-10">
                <div class="card organic-card">
                    <div class="card-body p-4">
                        <h5 class="fw-bold mb-4">Tambah Transaksi Baru</h5>
                        <form action="/add" method="POST" class="row g-3">
                            <div class="col-md-4">
                                <label class="form-label">Nominal (Rp)</label>
                                <input type="number" name="nominal" class="form-control" placeholder="50000" required>
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Kategori</label>
                                <input type="text" name="nama_kategori" class="form-control" placeholder="Makan, Main, dll" required>
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Tanggal</label>
                                <input type="date" name="tanggal" class="form-control" required>
                            </div>
                            <div class="col-md-8">
                                <label class="form-label">Keterangan Singkat</label>
                                <input type="text" name="keterangan" class="form-control" placeholder="Belanja bulanan...">
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Metode Pembayaran</label>
                                <select name="metode_pembayaran" class="form-select">
                                    <option value="Cash">Cash / Tunai</option>
                                    <option value="Bank/Transfer">Transfer Bank</option>
                                </select>
                            </div>
                            <div class="col-12 text-end mt-4">
                                <button type="submit" class="btn btn-organic">Simpan Transaksi</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>

        <div class="row justify-content-center pb-5">
            <div class="col-lg-11">
                <div class="table-container">
                    <div class="table-header-custom">Riwayat Pengeluaran</div>
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr class="text-center">
                                    <th>Tanggal</th>
                                    <th>Kategori</th>
                                    <th>Nominal</th>
                                    <th>Metode</th>
                                    <th>Keterangan</th>
                                    <th>Aksi</th>
                                </tr>
                            </thead>
                            <tbody class="text-center">
                                <% if (typeof data !== 'undefined' && data.length > 0) { %>
                                    <% data.forEach(item => { %>
                                    <tr>
                                        <td class="small text-muted"><%= item.tanggal %></td>
                                        <td><span class="badge-category"><%= item.nama_kategori %></span></td>
                                        <td class="fw-bold">Rp <%= item.nominal.toLocaleString('id-ID') %></td>
                                        <td class="text-secondary"><%= item.metode_pembayaran %></td>
                                        <td class="text-muted"><%= item.keterangan %></td>
                                        <td>
                                            <div class="d-flex justify-content-center gap-2">
                                                <a href="/edit/<%= item.PengeluaranID %>" class="btn btn-sm btn-edit px-3">Edit</a>
                                                <a href="/delete/<%= item.PengeluaranID %>" class="btn btn-sm btn-hapus px-3">Hapus</a>
                                            </div>
                                        </td>
                                    </tr>
                                    <% }) %>
                                <% } else { %>
                                    <tr>
                                        <td colspan="6" class="py-5 text-muted italic">Belum ada data transaksi.</td>
                                    </tr>
                                <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>